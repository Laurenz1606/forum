{
  "name": "backend",
  "version": "0.0.0",
  "description": "A simple forum for online communities. (backend)",
  "main": "dist/index.js",
  "repository": "https://github.com/Laurenz1606/forum.git",
  "author": "Laurenz1606 <laurenz.rausche@gmail.com>>",
  "license": "MIT",
  "private": false,
  "scripts": {
    "build:clean": "rimraf dist",
    "build:compile": "npm run dev:format && npm run build:clean && tsc",
    "build:copy": "rimraf ../out/backend/* && recursive-copy dist ../out/backend/dist && recursive-copy package.json ../out/backend/package.json",
    "build:full": "npm run build:compile && npm run build:copy && npm run build:clean",
    "console:clear": "node -e \"console.clear()\"",
    "dev:format": "prettier --write --config ../.prettierrc.json src",
    "dev:remove": "rimraf node_modules yarn.lock package-lock.json",
    "dev:start": "npm run console:clear && cross-env NODE_ENV=development ts-node-dev --require=dotenv/config --respawn --clear --watch .env src/index.ts",
    "start": "node ."
  },
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^18.11.18",
    "cross-env": "^7.0.3",
    "dotenv": "^16.0.3",
    "prettier": "^2.8.3",
    "recursive-copy-cli": "^1.0.20",
    "rimraf": "^4.1.1",
    "ts-node-dev": "^2.0.0",
    "typescript": "^4.9.4"
  }
}
